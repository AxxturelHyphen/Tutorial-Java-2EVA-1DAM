{
  "name": "java-educational-project",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "compile": "javac -d out src/**/*.java",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}